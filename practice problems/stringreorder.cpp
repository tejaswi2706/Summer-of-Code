#include<iostream>
#include<algorithm>
#include<vector>
using namespace std;
int main(){
    string s;
    cin>>s;
    int n=s.length();
    vector<char> v(n);
    for(int i=0;i<n;i++){
        v[i]=s[i];
    }
    
}